/**
 * Workflow task entry-point. `npm run start:tasks` runs this file; the
 * @renderinc/sdk picks up `export`ed tasks and registers them with Render.
 */
export { research } from "./research-task.js";
